services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: llamativo-admin-frontend-dev
    restart: unless-stopped
    ports:
      - "4200:80"
    shm_size: '1gb'
